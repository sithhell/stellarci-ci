build:
  - name: Building...
  - os:
    - linux:
      - x86_64
  - compiler:
    - gcc:
      - 8.3
  - step:
    - name: Build
    - commands:
      - spack list
      - gcc --version
  - step:
     - name: Test
     - commands:
       - find %(prop:repodir)s
